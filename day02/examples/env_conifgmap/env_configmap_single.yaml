apiVersion: v1
kind: Pod
metadata:
  name: env-config-single
spec:
  containers:
  - name: app
    image: nginx
    env:
    - name: CONFIGMAP_USERNAME
      valueFrom:
        configMapKeyRef:
          name: myconfigmap
          key: username